services:
  - type: web
    name: prime-trade
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATA_DIR
        value: /data
    disk:
      name: prime-trade-data
      mountPath: /data
      sizeGB: 1
