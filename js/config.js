/**
 * Supabase — Dashboard → Settings → API
 * (NEXT_PUBLIC_* names match common env naming; this app reads config here.)
 */
window.PRIME_TRADE_CONFIG = {
  STORAGE_MODE: 'supabase',
  SUPABASE_URL: 'https://ldvizxyeqafrenivmang.supabase.co',
  SUPABASE_ANON_KEY: 'sb_publishable_wobCSQ0TlyQnT2MehA9Wqw_nibPyzGR',
  /** Sponsor earns this fraction of each referred user's approved deposit */
  REFERRAL_COMMISSION_RATE: 0.05,
};
